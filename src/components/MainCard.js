import React, { Component } from 'react'

const MainCard = (props) => { //just make position relative to this one and move it around 
    console.log('MainCard: ', props);
    return ( 
        <div>MainCard</div>
     );
}
 
export default MainCard;